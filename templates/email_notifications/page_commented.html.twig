<div>

    {% set innerMailTitle = null %}

    {% if pageType is defined %}

        {% if pageType == "projectPresentation" %}

            {% set innerMailTitle = 'Votre page de projet "' ~ presentation.goal ~ '"' %}
            
        {% elseif pageType == "article" %}

            {% set innerMailTitle = 'Votre article "' ~ article.title ~ '"' %}
            
        {% endif %}

    {% endif %}

    <div>{{innerMailTitle}} a reçu un nouveau commentaire !</div>

    </br>

    <div>

        Message de {{newComment.user.username}}. </br>
        Envoyé le {{newComment.createdAt | date("d/m/Y \à h:i:s") }} </br>
        Message : {{newComment.content}}


    </div>

    </br>

    <div>

        Pour répondre à ce commentaire, <a href="{{ commentedPageUrl }}#comment-{{ newComment.id }}">cliquez ici</a>.

    </div>

    </br>
    
    <div>
        
        Pour toute question, remarque ou suggestion, <a href="{{ url ('contact_website') }}">cliquez ici</a>.
    
    </div>

    </br>

    <div>

        Merci d'utiliser Propon.

    </div>

</div>