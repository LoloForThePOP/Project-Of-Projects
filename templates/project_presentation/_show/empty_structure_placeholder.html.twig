{# 
    
    Placeholder "Skeleton", only displayed for user presenter, in edition mode, when a presentation structure is void (example: no websites; or no documents; etc.)
    
#}

    <a {# Placeholder can trigger a modal, or target a route #}

        {% if route is defined %}

            href="{{route}}"

        {% else %}
                
            data-bs-toggle="modal"
            
            data-bs-target="{{addElementModal ?? ""}}"
        
        {% endif %}

        class="pp-edition-mode"
    >

        <div class="placeholder-container">
    
            <img class="placeholder-image img-fluid" src="{{ asset(iconName ~'.svg', 'pp_wysiwyg')}}" alt="Placeholder for {{entityType | default ("an empty structure")}}" width="200" height="200">

            <div class="placeholder-label">

                {{label | default ("")}}

            </div>
    
        </div>

    </a>


    <style>

        .placeholder-container{

            position: relative;

            display: flex;
            justify-content: center;
            align-items: center;

            height: 100%;

            cursor: pointer;

        }

        .placeholder-container:hover{

            background-color: rgb(239, 239, 248);

        }

        .placeholder-image{
            z-index: 2;
        }

        .placeholder-label{

            position: absolute;
            top:0; 
            left:0;

            width: 170px;
            height:50px;

            font-size: 18px;
            font-weight: bold;

            color: black;

        }

    </style>
