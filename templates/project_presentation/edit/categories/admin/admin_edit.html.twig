{% extends 'base.html.twig' %}


{% block title %}

    {% if context == 'new' %}

        Ajouter

    {% else %}

        Modifier

    {% endif %}
    
        une catégorie
    
    {% endblock %}

{% block sidebar %}{% endblock %}

{% block body %}

<div class="container">

    <!-- Title with Info Button Container -->

    <div class="mt-3 mt-md-4 mb-md-3">

        <!-- ui Page Title -->

        <h3 class="fw-bold d-inline me-2">

            {% if context == 'new' %}

                Ajouter

            {% else %}
                
                Modifier
            
            {% endif %}

            une catégorie
            
        </h3>

        <!-- Button to trigger instructions (collapsible) 

        <a class="fw-bold my-1" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTutorial"
            aria-expanded="false" aria-controls="collapseTutorial">

            <span class="btn btn-info fw-bold btn-lg fs-15 h5 p-1 px-3" style="letter-spacing: 2px;">Instructions</span>

        </a>-->

    </div>

    <!-- Collapsible Info Container

    <div id="collapseTutorial" class="collapse no-transition col-md-7 px-4 py-3 mb-4"
        style="background-color: lavender;">

        none

    </div> -->

    <!-- Form Container -->

    <div class="mt-2">

        {{form_start(form)}}

        {{form_widget(form)}}


        <!-- Action Buttons -->

        <div class="mt-4 pt-2 pl-0">

            <button type="submit" class="btn btn-success bg-green sharp btn-lg h3 mb-4 me-2">Valider</button>

            <a class="text-decoration-none text-white" href="{{ path('homepage')}}">
                <button type="button" class="btn btn-info btn-lg mb-4 me-2">Annuler</button>
            </a>

            {% if context == 'update' %}
                
                <a class="text-decoration-none text-white" href="{{ path('delete_category', {'id': cat_id})}}">
                    <button type="button" class="btn btn-danger btn-lg mb-4 me-2">Supprimer</button>
                </a>
            
            {% endif %}

        </div>

        {{form_end(form)}}

    </div>

</div>


{% endblock %}