<script>

  const search = instantsearch({
    indexName: 'dev_presentation_bases',
    searchClient: algoliasearch(
      'Z7NO8ZLFH4',
      'b2d9ba779ea94f1c5f81d1b5751e267e'
    ),
  });
  
  search.addWidgets([
    instantsearch.widgets.searchBox({
      container: '#searchbox',
    }),
    instantsearch.widgets.clearRefinements({
      container: '#clear-refinements',
    }),
    instantsearch.widgets.refinementList({
      container: '#brand-list',
      attribute: 'categories.descriptionFr',
    }),
    instantsearch.widgets.hits({
      container: '#hits',
      templates: { /* Twig verbatim used to avoid curly brackets (braces) conflicts */
        item: `{% verbatim %}
          <div>
            <div class="hit-name">
              {{#helpers.highlight}}{ "attribute": "title" }{{/helpers.highlight}}
            </div>
            <div class="hit-description">
              {{#helpers.highlight}}{ "attribute": "goal" }{{/helpers.highlight}}
            </div>
          </div>
        {% endverbatim %}`,
      },
    }),
    instantsearch.widgets.pagination({
      container: '#pagination',
    }),
    instantsearch.widgets.configure({
      hitsPerPage: 8
    }),
  ]);
  
  search.start();
  
</script>