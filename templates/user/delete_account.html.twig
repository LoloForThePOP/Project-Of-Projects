{% extends 'base.html.twig' %}

{% block title %}Supprimer un compte utilisateur{% endblock %}

{% block body %}

    {#
        
        Processus :

            - js : we ask user to confirm account deletion by typing 'confirm'

            - js : we check if user typed 'confirm' (javascript). If so, form validation event is not prevented.
        
    #}

    <div class="container pt-md-4 pb-md-5">

        <div class="row mt-5">

            <div class="col-md-7">

                <div class="h4 fw-bold mb-5">
                    Supprimer le compte utilisateur
                </div>

                <div class="alert alert-danger d-flex align-items-center mb-3">

                    {# warning icon #}

                    <svg class="me-3" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="bi bi-exclamation-triangle-fill flex-shrink-0 me-2" viewBox="0 0 16 16" role="img" aria-label="Warning:">
                        <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                    </svg>

                    <div class="fw-bold">
                        Cette action supprimera les données de votre compte et les présentations de projet si vous en avez créés.
                    </div>
                    
                </div>

                <div class="alert alert-success mb-5" role="alert">
                    <h4 class="alert-heading">Améliorer le site</h4>
                    <p class="fw-bold">Si vous avez rencontré des problèmes avec le site, nous en sommes désolés. Donnez-nous votre avis, et laissez-nous une chance de l'améliorer.</p>
                    <hr>
                    <p class="fw-bold mb-0">Utilisez <a href="{{ path ('contact_website') }}" class="text-decoration-none">ce formulaire de contact</a> . Nous vous recontacterons dans les délais les plus brefs, et nous réagirons pour trouver une solution.</p>
                  </div>

                {{ form_start(form) }}
            
            
                {{ form_widget(form) }}

                {# Delete Presentation Button #}

                <button id="deleteProjectLink" onclick="confirmDeletion()" class="btn btn-danger mb-2 me-3">Supprimer mon compte utilisateur</button>

                {# Back Button #}

                <a class="text-decoration-none text-white" href="{{path("show_user_profile", {'userNameSlug': app.user.userNameSlug})}}">
                    
                    <button type="button" class="btn btn-go-back mb-2 me-2">Annuler</button>
                
                </a>

                {{ form_end(form) }}

                        

            </div>

        </div>

    </div>

{% endblock %}

{% block javascripts %}

    <script>

        function confirmDeletion() {

            var userConfirmation = prompt("Attention, cette action est irréversible. Pour confirmer, écrire confirmer", "Écrire ici");

            if (userConfirmation !== "confirmer" && userConfirmation !== "Confirmer") {
                
                event.preventDefault();
            } 

        }

    </script>

{% endblock %}